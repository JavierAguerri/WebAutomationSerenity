serenity {
    take.screenshots = FOR_FAILURES
    project.name = "QA Demo"
    restart.browser.for.each = "scenario"
    reports {
        show.step.details = true
    }
}
environments {
    dev {
        webdriver.base.url = "https://www.saucedemo.com/"
        webdriver.driver = firefox
    }
}
